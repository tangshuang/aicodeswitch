# 日志清理功能

请求日志长期累积会占用磁盘。在 Web UI「日志」页右上角可查看占用空间并按日期清理。

## 用法

1. 点击右上角「清理」按钮。
2. 柱状图展示每日占用（可切换「按周」）；选择「截止日期」，该日及之前的柱子变红，汇总会显示将清理的条数与释放空间。
3. 「确认清理」→ 二次确认，删除选中日期（含当天）及之前的全部日志分片（不可撤销）。

清理范围覆盖所有日志：主库 `global` + 接入密钥 `key:*`。日志默认不会被自动清理，可保留任意时长，请按需手动清理。
